You are given the full text of a technical document.

Return ONLY valid JSON (no preface, no markdown). Use this exact schema:
{
  "headers": [
    { "title": "string (exactly as written in the document, preserve spacing/case/linebreaks if present)",
      "level": 1|2|3,
      "page": integer (1-based page number) }
  ]
}

Rules:
- Include ALL top-level headers AND their subheaders (levels 1–3).
- Titles must match the document text EXACTLY (respect spacing and line breaks).
- Page is where the header’s visible title appears in the main body (ignore TOC, index, running headers).
- Do not include any other keys. Do not include commentary. Output must be a single JSON object.
